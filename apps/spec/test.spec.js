
(function() {
    
    // テストするソースの読み込み
    var UploadJs = require('../routes/upload.js');
    
    // テストコード
    describe('Upload Routing', function(){
        it('存在するファイルを指定した場合は結果が帰ってくるか？', function() { 
            console.log("ok");
            //expect(undefined).not.toBe(undefined);
            
        });
    });
})();